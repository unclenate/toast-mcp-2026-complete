<!--
Copyright 2026 Nate DiNiro <nate@bdits.io>
SPDX-License-Identifier: MIT OR Apache-2.0
-->

# Project Change Log — Toast MCP Server (hardened fork)

This log records material changes to project scope, plan, timeline, or technical direction.
It is not a git commit log — it captures decisions and their rationale, not code diffs.

| Date | Type | Change | Reason | Owner | ADR/PRD |
| ---- | ---- | ------ | ------ | ----- | ------- |
| 2026-05-28 | Scope | Adopted auto-harness governance under composition `mcp-server-typescript-oss` (.harness@4647a5f). Manifest pinned; kernel artifacts created; MCP module artifacts seeded from 2026-05-27 security audit; risk register populated with 8 findings; ADR-0001 records fork rationale. | Phase B of the harness adoption track (Track 1). The fork existing (vs. accepting upstream `@busybee3333/toast-mcp-server` posture) requires explicit governance to prevent us inheriting upstream's drift. | @unclenate | ADR-0001 |
| 2026-05-29 | Direction | ADR-0002 accepted: read-only mode becomes the default via `TOAST_READ_ONLY` env flag, gated by a per-tool `mutates: boolean` tag. Mitigates RISK-006. M2 moves to in progress; implementation lands as a separate Tier-2 workspace edit reviewed by the maintainer. | Operational target is local stdio against Toast sandbox; safe-by-default write suppression is a prerequisite before any credentials reach the process. Also unblocks M5 release scaffolding. | @unclenate | ADR-0002 |
| 2026-05-29 | Risk | RISK-009 (lockfile drift) added to docs/mcp/risk-register.md. Discovered while preparing the stdio dev path: upstream lockfile resolved express 5.x while package.json declares ^4.18.2, causing `npm ci` to fail with EUSAGE. Mitigation deferred to Track 2 (M5 CI verifies the lock). | Documenting the finding under the same companion-rule discipline as RISK-001..008 so the v0 release scaffolding can address it deterministically. | @unclenate | (no ADR — Track 2 housekeeping) |
| 2026-05-29 | Code | Track 3 M2 implementation merged to main as PR #3 (squash commit `467e164`). 21 write tools tagged `mutates: true`; `TOAST_READ_ONLY` strict-parsed env flag gates registration; dispatch-time defense-in-depth gate added; named skipped tools logged at startup; new `src/lib/error-info.ts` helper for batch handlers. Runtime validated against all 6 ADR-0002 parser branches. Mitigates RISK-006. | M2 was the prerequisite for any sandbox-credentials work — safe-by-default write suppression must land before real Toast tokens reach the process. | @unclenate | ADR-0002 |
| 2026-05-29 | Code | Four pre-existing upstream TypeScript defects fixed in the same PR #3, surfaced for the first time once RISK-009's lockfile drift was resolved and `tsc --strict` could run: (1) `src/types/index.ts` Order interface had duplicate `source`/`deliveryInfo`/`curbsidePickupInfo` declarations; (2) `src/tools/orders.ts:308` had an operator-precedence bug where the email branch dereferenced `check.customer` without the guard; (3,4) `src/tools/inventory.ts` + `src/tools/menus.ts` bulk handlers used `'error' in r` on `unknown[]` (TS18046 plus a runtime ambiguity where a successful Toast response containing an `error` field would be misclassified). Refactored to discriminated `{ok: true} | {ok: false}` union via per-update try/catch. | Not in ADR-0002's scope, but blocking on the M2 build validation. These defects pre-existed at b913e19; RISK-009 had been masking them by preventing anyone from running `tsc`. | @unclenate | (no ADR — pre-existing upstream bugs surfaced incidentally) |
| 2026-05-29 | Direction | ADR-0003 accepted: monetary input bounds at the Zod boundary via `z.number().int().positive().max(MAX_MONETARY_CENTS)` (and bounded-magnitude variant for the bipolar cash entry). `MAX_MONETARY_CENTS` defaults to `10_000_000` ($100K) and is env-overridable via `TOAST_MAX_MONETARY_CENTS` (strict-parsed). Mitigates RISK-001, RISK-002, and the newly-added RISK-010 (four unaudited monetary fields surfaced during M3 inventory). M3 moves to in progress; implementation lands as a separate Tier-2 workspace edit. | Operating principle 3 promised this shape but no code enforced it. The audit caught 3 fields; an M3-discovery grep against `z.number()` in `src/tools/*.ts` surfaced 4 more — same defect class, unaudited. The new findings are bundled as RISK-010 (single row, multi-field) under the ADR-0001 precedent of "one ADR per design, not per finding." | @unclenate | ADR-0003 |
| 2026-05-30 | Code | Track 3 M3 implementation merged to main as PR #6 (squash commit `30315217`). New `src/lib/monetary.ts` (env-parse + `positiveCents()` / `boundedMagnitudeCents()` helpers); 7 monetary fields across payments/cash/menus bounded. A follow-up review-fix commit hardened the `TOAST_MAX_MONETARY_CENTS` parser against permissive `Number()` shapes (scientific notation, hex, whitespace, >2^53) via a `/^[1-9]\d*$/` + `Number.isSafeInteger` two-gate parse. Closes RISK-001, RISK-002, RISK-010. | M3 was the last functional Track 3 milestone blocking the M5 release scaffolding (alongside M4). The parser-hardening fix came from a two-reviewer convergent finding — `Number()` silently accepted `1e10` as a 1000× cap raise. | @unclenate | ADR-0003 |
| 2026-05-31 | Direction | ADR-0004 accepted: HTTP transport hardening. (1) default bind `127.0.0.1` via `TOAST_MCP_HOST` (opt-in `0.0.0.0`); (2) deny-all CORS by default, allow-list via `TOAST_CORS_ORIGINS` (no middleware registered when unset, so same-origin `/apps` UI is unaffected); (3) `/health` returns only `{status:'ok'}`. New `src/lib/http-config.ts` helper. Mitigates RISK-003, RISK-004, RISK-005. M4 moves to in progress; implementation lands as a separate Tier-2 workspace edit. | Wrong-by-default HTTP posture (all-interfaces bind + wildcard CORS + version disclosure) blocks the M5 release even though HTTP tool execution is still a stub. Hardening now means the mode is safe the moment it's wired up. Reuses the strict-env-parse-with-safe-default pattern from ADR-0002/0003. | @unclenate | ADR-0004 |
